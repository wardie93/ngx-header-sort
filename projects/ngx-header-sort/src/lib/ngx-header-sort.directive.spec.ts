import { NgxHeaderSortDirective } from './ngx-header-sort.directive';

describe('NgxHeaderSortDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxHeaderSortDirective();
    expect(directive).toBeTruthy();
  });
});
